#Users.insert(
    struct { id: int32, name: string, active: bool } {
        id: - 0i32 1_i32, // 0 - 1 instead of just -1 as negation operator isn't finished
        name: "El Jones, Jim",
        active: false
    }
)
