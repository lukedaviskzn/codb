// `UserStatus.Inactive` and `UserStatus.Inactive(unit)` are equivalent
match UserStatus.Inactive {
    // `Active(_) => ...` and `Active => ...` are equivalent
    Active(_) => "A",
    Inactive => "B",
    Other(_) => "C",
} -> string
